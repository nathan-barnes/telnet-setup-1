session_name: telnet-client
suppress_history: false
windows:
  - layout: main-vertical
    panes:
      - yarn run start
      - ${EDITOR} -S
  - panes:
      - shell_command:
          - git fetch
          - git status
